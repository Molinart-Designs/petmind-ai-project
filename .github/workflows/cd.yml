name: CD

# E3: conectar despliegue a AWS (ECS, App Runner, etc.) y secretos del repositorio.
on:
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO(E3): desplegar imagen Docker a la nube."
